To install the Crypt package you currently have to make a distribution
tarball first:

$ pear package package.xml

This will result in a file called CRYPT-0.1.tgz that you have to install:

$ pear install CRYPT-0.1.tgz

Now you are all set.
